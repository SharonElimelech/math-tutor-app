# 📐 המורה שלי — אפליקציה לניהול שיעורים פרטיים

אפליקציית **PWA** (Progressive Web App) לניהול שיעורים פרטיים: תלמידים, יומן, תזכורות תשלום בוואטסאפ וסיכום הכנסות. עובדת בדפדפן, ניתנת להתקנה בטלפון כמו אפליקציה רגילה, ועובדת גם בלי אינטרנט. כל הנתונים נשמרים מקומית במכשיר (localStorage) — אין שרת ואין עלויות.

## יכולות

- **מסך "היום"** — ברכה, השיעור הבא, שורת "לטיפול" (שיעורים לאישור, תזכורות, גבייה) והיומן במסך אחד.
- **ניהול תלמידים** — שם, שם הורה, טלפון, מחיר לשיעור, פעולות מהירות (וואטסאפ, קביעת שיעור) על הכרטיס.
- **יומן שיעורים** — תצוגות יום / 3 ימים / שבוע / חודש / רשימה, גרירת שיעורים, פס סטטוס תשלום על כל שיעור, כפתור "+" צף.
- **תזכורות בדפדפן** — התראה ~30 דקות לפני שיעור (כשהאפליקציה פתוחה ברקע).
- **תזכורות תשלום בוואטסאפ** — האפליקציה מכינה הודעה עם הסכום ופותחת את וואטסאפ עם ההודעה מוכנה; את רק לוחצת "שלח".
- **סיכום הכנסות** — דוח חודשי (התקבל / ממתין), השוואה לחודש הקודם וגרף SVG של 6 חודשים אחרונים (תומך במצב כהה).

## הרצה מהירה במחשב

מכיוון שיש Service Worker, צריך להריץ דרך שרת מקומי (לא לפתוח את הקובץ ישירות):

```bash
# בתיקיית הפרויקט:
python3 -m http.server 8000
# ואז לפתוח בדפדפן: http://localhost:8000
```

או עם Node:
```bash
npx serve .
```

## התקנה בטלפון

1. העלי את התיקייה לאחסון חינמי שתומך ב-HTTPS, למשל **Netlify** (גרירה של התיקייה ל-netlify.com/drop) או **GitHub Pages**.
2. פתחי את הכתובת בטלפון בכרום/ספארי.
3. בתפריט הדפדפן בחרי **"הוספה למסך הבית" / "Add to Home Screen"**.
4. האייקון יופיע במסך הבית ויפתח במסך מלא כמו אפליקציה.

## טכנולוגיות

HTML, CSS, Vanilla JavaScript, PWA (manifest + service worker), localStorage, Web Notifications API, SVG (לגרף), WhatsApp Click-to-Chat API.

## מבנה הקבצים

```
index.html          # מבנה, ניווט תחתון (4 מסכים) וכפתור פעולה צף
styles.css          # עיצוב RTL רספונסיבי, מצב כהה, טוקנים
app.js              # מסכים, יומן, מרכז התזכורות, וואטסאפ, גרף
src/                # מודולים טהורים: נתונים, יומן, סלקטורים, תזכורות, אחסון, push
tests/              # node --test
manifest.json       # הגדרות PWA
service-worker.js   # תמיכה offline + התקנה
icon-192/512.png    # אייקונים
```

---
 מוכן ל-CV / לינקדאין

> **המורה שלי — אפליקציית PWA לניהול שיעורים פרטיים** · _פרויקט אישי_
> פיתחתי אפליקציית ווב מתקדמת (PWA) מקצה-לקצה לניהול עסק של הוראה פרטית: ניהול תלמידים, יומן שיעורים עם תזכורות, מעקב תשלומים אוטומטי דרך WhatsApp וסיכום הכנסות חודשי עם ויזואליזציה גרפית. עבודה עם JavaScript, Web APIs (Notifications, Service Worker, Canvas) ושמירת נתונים מקומית, ללא צורך בשרת. האפליקציה ניתנת להתקנה בטלפון ועובדת גם offline.


